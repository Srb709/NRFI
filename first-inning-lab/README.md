# FIRST INNING LAB

Setup:
1. npm install
2. npm run dev
3. cd python
4. python -m venv .venv
5. Windows: .venv\Scripts\activate
6. pip install -r requirements.txt
7. python -m first_inning_lab.scripts.predict_today

Demo mode runs without API keys.
